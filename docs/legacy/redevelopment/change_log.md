# MAC Testbench Migration Change Log

| Phase | File | Change | Reason | AXI reference | MAC implementation | Risk | Validation | Status |
|---|---|---|---|---|---|---|---|---|
| 0 | `docs/axi_tb_inventory.md` | Added AXI reference inventory. | Establish evidence-backed framework baseline without modifying AXI. | AXI structure, Questa Makefile, regression helper, scoreboard, coverage subscribers. | Documentation only in MAC root. | None. | AXI was read-only; MAC source/sim dependency scan found no AXI references. | DONE |
| 0 | `docs/mac_tb_inventory.md` | Added MAC baseline inventory. | Identify existing strengths/gaps before refactoring. | Comparison against AXI framework patterns. | Documentation only in MAC root. | None. | Read-only MAC inspection. | DONE |
| 0 | `docs/axi_to_mac_migration_matrix.md` | Added component-by-component reuse/adapt/reject decisions. | Prevent protocol-specific copying. | AXI TLM FIFO, build, regression, and coverage patterns. | MAC-specific migration decisions. | None. | Reviewed against target source/filelist. | DONE |
| 1 | `docs/mac_tb_target_architecture.md` | Added implementation contract for scoreboard, configuration, artifacts, reporting, and coverage. | Stage safe MAC-local implementation. | Generic AXI framework organization only. | Preserves MAC predictor/configuration architecture. | None; no source change. | Documentation review; no AXI dependency introduced. | DONE |
| 2 | `src/hvl_top/tb/mac_env_config.sv` | Added MAC scoreboard policy fields for TX/RX/field checking, unmatched timeout, and reset flushing. | Make checker behavior explicit and test-configurable. | Generic AXI configuration-object pattern. | Preserves strict in-order MAC packet checking by default. | Medium: changed checker policy/config contract. | Questa compile PASS; `mac_smoke_test_c` PASS; `mac_tx_payload_50_test_c` PASS; zero UVM errors/fatals. | DONE |
| 2 | `src/hvl_top/tb/mac_sb.sv` | Added environment configuration lookup, reset analysis implementation, reset queue flushing, timeout accounting, configurable field checks, and expanded summary. | Make expected/actual packet checking reset-aware and diagnosable. | AXI analysis FIFO, comparison counter, and report-phase concepts. | MAC-specific predictor/FIFO/packet comparison retained. | Medium: reset behavior and checker policy. | Questa compile PASS; smoke PASS; deterministic TX payload test PASS with `tx_matches=1`, `mismatches=0`. | DONE |
| 2 | `src/hvl_top/tb/mac_tb.sv` | Connected existing reset-monitor analysis ports to scoreboard. | Establish comparison-epoch boundary on reset. | Generic analysis-port-to-consumer pattern. | Existing MAC reset agent remains the source. | Low/medium: only active when reset is asserted. | Smoke topology confirms two reset connections/epochs; zero UVM errors/fatals. | DONE |
| 3 | `sim/Questasim/mac_result_summary.py` | Added MAC-local UVM log parser and regression summary generator. | Produce per-test test/seed/status/error/warning/log/duration metadata. | AXI regression log/result collection concept. | No AXI script reused; JSON and text summary are MAC-local. | Low: automation only. | Parsed deterministic TX and smoke logs as PASS with zero errors/warnings. | DONE |
| 3/4 | `sim/Questasim/Makefile` | Added configurable artifact/argument variables, compile/test/transaction log paths, result capture, smoke alias, coverage alias, and result-aware regression flow. | Align public execution interface with migration contract. | AXI Makefile's compile/simulate/regression/coverage pattern. | Retains MAC targets and source filelist; no AXI sources/paths. | Medium: shell/Make integration. | Direct Questa compile and smoke PASS; result/log routing validated. GNU Make unavailable in this host, so target parsing/execution remains pending on a Make-enabled host. | PARTIAL |
| 3 | `.gitignore` | Ignores generated logs, results, and coverage roots. | Prevent generated framework artifacts from entering source control. | Per-test artifact separation concept. | MAC-local paths only. | None. | File review. | DONE |
| 3/4 | `sim/Questasim/Makefile` | Set default run artifacts to `./<mac_test>_<DDMMYYYY-HHMMSS>/`: `<test>.log`, `waveform.wlf`, transaction trace, and—on coverage runs—`<test>_coverage.ucdb` plus `html_cov_report/`. | Match the requested AXI-style per-test artifact folder format without copying AXI content. | AXI artifact layout pattern only. | MAC-local test names, paths, and metadata remain independent. | Low: output path convention only. | AXI reference directory inspected; GNU Make execution pending on a Make-enabled host. | PARTIAL |
| 3/4 | `sim/Questasim/Makefile`, `.gitignore` | Enabled recursive signal logging for WLF generation and ignored timestamped Questa run folders. | Ensure each MAC artifact directory includes the requested waveform while remaining generated output. | AXI waveform artifact convention only. | `log -r /*` is issued in MAC run flows; no AXI script used. | Low: simulation artifact behavior only. | Coverage smoke PASS; verified log, WLF, UCDB, HTML report, and transaction trace in one MAC timestamped folder. | DONE |
| 7 | `src/hvl_top/test/`, `src/hvl_top/testlists/`, `sim/Questasim/mac_compile.f`, `sim/Questasim/Makefile` | Reorganized MAC-local agent sequences under `test/sequences/`, virtual test sequences under `test/virtual_sequences/`, and regression list under `testlists/`; added corresponding include/testlist paths. | Adopt AXI test-layer organizational pattern without copying AXI protocol code. | AXI test/sequences/virtual_sequences/testlists structure only. | MAC AXI4-Stream, RS, and virtual-sequence classes were moved intact; virtual sequencer remains environment infrastructure. | Medium: compilation include paths and Makefile default testlist. | Questa compile PASS; `mac_smoke_test_c` PASS; UVM_ERROR=0; UVM_FATAL=0. | DONE |
| 7 | `src/hvl_top/test/mac_base_test.svh`, `mac_tx_payload_50_test.svh`, `docs/mac_test_style_guide.md` | Applied a MAC-specific base/derived test template: include guards, explicit phase/configuration documentation, factory sequence launch, configuration reporting, and clear completion/PASS reporting. | Provide a consistent, understandable test coding example. | AXI base/concrete test organization and phase/style pattern only. | MAC reset/APB bootstrap, AXI4-Stream/RS sequence, and scoreboard behavior preserved. | Low: reporting/style plus include guards. | Questa compile PASS; directed payload test PASS; `tx_matches=1`, `mismatches=0`, UVM_ERROR=0, UVM_FATAL=0. | DONE |
| 5 | `sim/Questasim/Makefile` | Corrected regression `RUN_ID` shell expansion from `$$t_` to `$${t}_$(RUN_TIMESTAMP)`. | Prevent all regression tests from sharing the malformed `regression_` artifact directory. | Generic timestamped per-test regression isolation. | Each MAC test now receives its own `<test>_<timestamp>` run directory. | Low: regression artifact path only. | Pending Make-enabled regression rerun. | IN PROGRESS |
| 5 | `sim/Questasim/Makefile` | Added configurable `UVM_TIMEOUT` (default `10ms`) to batch/coverage/GUI test invocations. | Ensure unattended regression tests end automatically; a stalled test becomes a recorded timeout failure and regression proceeds. | Generic UVM timeout/run-to-completion pattern. | MAC tests retain objection-based normal completion and `run -all; exit`. | Low: run-control safety net. | Pending Make-enabled regression rerun. | IN PROGRESS |
| 5 | `sim/Questasim/Makefile` | Corrected the timeout default to numeric `1000000` (1 ms in the MAC top-level's 1 ns timebase). | Questa UVM 1.1d interpreted the `10ms` string as 10 ns. | Simulator-compatible UVM timeout configuration. | Normal MAC objection-based completion remains unchanged. | Low: run-control safety net. | Pending timeout-enabled smoke and Make-enabled regression rerun. | IN PROGRESS |
| 5 | `sim/Questasim/Makefile` | Added `-c` through `VSIM_BATCH_OPTS` to batch, optimized batch, coverage, and regression execution paths. | Prevent regression from opening the Questa GUI; each test must run and exit unattended. | Generic batch simulator invocation. | GUI targets remain explicitly interactive; MAC regression uses batch-only targets. | Low: simulator invocation mode. | Direct `vsim -c` smoke PASS; Make-enabled regression rerun pending. | PARTIAL |
| 7 | `src/hvl_top/test/mac_base_test.svh`, `mac_sanity_test.svh`, `mac_payload_tests.svh`, `src/hvl_top/pkg/mac_test_pkg.sv`, `src/hvl_top/testlists/mac_regression.list` | Replaced the previous concrete tests with a single maintainable base, a reset/configuration sanity test, and parameterized 46/50/64-byte directed TX payload examples. | Establish one clear MAC-local test coding pattern for future tests. | AXI base-test/concrete-test separation and testlist organization only. | MAC-specific reset/APB bootstrap, frame construction, RS monitoring, and packet scoreboard checks are retained. | Medium: new test setup and regression selection. | Questa compile PASS; all four tests ran with `vsim -c` and UVM timeout protection; UVM_ERROR=0, UVM_FATAL=0; each payload test had `tx_matches=1`, `mismatches=0`. | DONE |
| 3 | `src/hvl_top/pkg/mac_txn_logger.svh`, `sim/Questasim/Makefile` | Changed transaction tracing from a separate `*.transactions.log` file to append into the existing per-test Questa/UVM log. | Keep one complete log artifact per test. | Generic single-test log collection pattern. | Every normal, optimized, coverage, and GUI target passes its existing `RUN_LOG` via `+MAC_TXN_LOG`; logger opens it in append mode. | Low: log destination only. | Batch payload test PASS; one log contained UVM summary and `DRIVE_*`/`OBSERVE_*` trace entries; no separate transaction log was generated. | DONE |
| 4 | `sim/Questasim/Makefile` | Updated the default and `make smoke` target to select `mac_sanity_test_c`. | The former smoke test source was deliberately retired. | Generic smoke-test alias pattern. | MAC sanity test is the first test in the MAC-local regression list. | Low: target selection only. | Make execution is pending because GNU Make is not installed on this host; direct batch sanity run PASS. | PARTIAL |
| 7 | `src/hvl_top/test/mac_parallel_tx_rx_test.svh`, `src/hvl_top/pkg/mac_test_pkg.sv`, `src/hvl_top/testlists/mac_regression.list`, `src/hvl_top/pkg/mac_wait_utils.svh` | Added concurrent AXI-client TX and RS-line RX traffic test plus an AXI-clock-aware bounded completion wait. | Exercise both independent MAC data paths concurrently using deterministic, valid frames. | Generic parallel-sequence and bounded-wait patterns. | Passive line/client monitors and existing MAC reference model/scoreboard validate both directions. | Medium: concurrent traffic exposes timing defects. | Questa compile PASS; UVM_ERROR=0, UVM_FATAL=0; scoreboard `tx_matches=1`, `rx_matches=1`, `mismatches=0`. | DONE |
| 7 | `src/hvl_top/test/mac_pause_test.svh` (validation only) | Ran RX and TX PAUSE tests. | Validate PAUSE control behavior. | MAC-specific PAUSE checking. | RX PAUSE passes; TX PAUSE is retained as a failing diagnostic. | High: TX PAUSE wire format is noncompliant. | RX: PASS, zero UVM errors/fatals. TX: failure observed because DUT PAUSE output lacks RS preamble/FCS and produces an unmatchable 34-byte payload; no RTL modification made. | BLOCKED: RTL fix required |
| RTL | `src/hdl_top/pause/pause_tx.sv` | Changed generated PAUSE traffic from an unframed 60-octet control body to a two-beat, 72-octet line-side image containing preamble, SFD, control body, and generated FCS. | PAUSE TX must satisfy the same RS wire-format contract as normal Ethernet frames. | MAC-specific PAUSE frame requirement. | CRC is calculated over DA through pad; the second beat contains the final four body pad bytes and four FCS bytes. | Medium: PAUSE output timing changed from one beat to two. | Questa compile PASS; `mac_pause_tx_test_c` PASS with `tx_matches=1`, `mismatches=0`, UVM_ERROR=0, UVM_FATAL=0. | DONE |
| RTL | `src/hdl_top/tx/tx_arbiter.sv` | Handshaked the PAUSE source on the IDLE-cycle output beat. | Prevent the first PAUSE beat from being emitted once without source acknowledgement and then emitted again in the PAUSE state. | Ready/valid handshake rule. | IDLE transitions directly to PAUSE only when additional PAUSE beats remain. | Medium: PAUSE arbiter state transition. | `mac_pause_tx_test_c` PASS; one correct PAUSE frame observed and matched. | DONE |
| RTL | `src/hdl_top/tx/tx_axi4_stream_adapter.sv` | Refined AXI source-stability assertion to check fields across consecutive stalled cycles. | A transfer completing on the following ready edge is legal and must not be reported as a stall violation. | AXI ready/valid stability rule. | Functional datapath unchanged; assertion now covers sustained backpressure. | Low: assertion-only change. | `mac_parallel_tx_rx_test_c` PASS with simulator Errors=0, UVM_ERROR=0, UVM_FATAL=0, `tx_matches=1`, `rx_matches=1`. | DONE |
| 4/5 | `sim/Questasim/Makefile`, `sim/Questasim/mac_result_summary.py` | Scoped regression JSON files to a unique regression ID; added run duration capture and non-UVM simulator-error detection. | Prevent stale result aggregation and false PASS reports. | Generic regression result isolation/reporting pattern. | WSL `make regression` reports exactly its 10 current tests; individual results include elapsed seconds. | Low: automation only. | WSL regression PASS, 10/10 tests. | DONE |
| 6 | `src/hvl_top/test/mac_base_test.svh`, `sim/Questasim/Makefile` | Enabled functional coverage when `run_cov` passes `+MAC_FUNCTIONAL_COVERAGE`. | Simulator coverage collection must include the MAC covergroups. | Generic coverage-enable control. | Functional subscriber is enabled by a coverage-only plusarg. | Low: coverage collection only. | WSL `make run_cov` PASS; UCDB reports 35.14% covergroup coverage for concurrent TX/RX test. | DONE |
| 7 | `src/hvl_top/test/mac_payload_tests.svh`, `mac_register_access_test.svh`, `src/hvl_top/pkg/mac_test_pkg.sv`, `src/hvl_top/testlists/mac_regression.list` | Added basic VLAN TPID TX, 1500-byte payload TX, and legal APB fixed/group-map access sweep tests. | Expand baseline feature regression beyond minimal payload and PAUSE paths. | MAC-local feature testing. | Tests reuse the shared base, packet scoreboard, APB agent, and batch automation. | Medium: expanded traffic/configuration scenarios. | WSL regression PASS, 10/10 tests with UVM_ERROR=0/UVM_FATAL=0. | DONE |
| 7 | `src/hvl_top/tb/mac_ral.svh`, `src/hvl_top/test/mac_base_test.svh`, `src/hvl_top/test/mac_register_access_test.svh` | Added MAC APB RAL field metadata, the APB register adapter, virtual-sequencer binding, group-table entries, and a RAL frontdoor read. | Turn the address-only RAL into an executable MAC-local register model with documented RO/RW/W1C semantics. | Generic UVM register-model, adapter, and frontdoor patterns. | Uses the existing APB transfer/driver only; no AXI artifact or path is referenced. | Medium: RAL/sequence integration. | WSL `make run TEST=mac_register_access_test_c`: PASS; 93 APB transfers, RAL version frontdoor read PASS, UVM_ERROR=0, UVM_FATAL=0. | DONE |
| RTL | `src/hdl_top/registers/apb_cfg_bridge.sv`, `src/hdl_top/registers/apb_regs.sv` | Added APB-side PAUSE-TX configuration readback and made bridge diagnostic `$display` output opt-in via `+MAC_CFG_BRIDGE_DEBUG`. | Expose the programmable PAUSE register coherently to software/tests and prevent unsolicited debug text from polluting normal per-test logs. | Generic register readback and controlled-debug logging practice. | PAUSE configuration remains transferred to the MAC domain through the existing CDC bridge. | Low/medium: APB register visibility only. | WSL semantic APB/RAL test PASS; PAUSE configuration readback checked; normal log contains no CFG_BRG debug spam. | DONE |
| Final validation | `sim/Questasim/Makefile`, `src/hvl_top/testlists/mac_regression.list` | Executed the complete MAC non-coverage regression in batch mode. | Confirm handoff behavior after RAL, APB readback, and log-cleanliness changes. | Generic unattended regression completion and result summary. | `make regression SEED=20260809 REGRESSION_ID=noncoverage_handover_20260809`. | Low: validation only. | PASS: 10/10 tests, UVM_ERROR=0, UVM_FATAL=0, parser errors=0, warnings=0; all artifact/log paths isolated. | DONE |
| 2/6/7 | `src/hvl_top/tb/mac_env_config.sv`, `mac_tb.sv`, `mac_ral.svh`, `mac_ref_model.sv`, `mac_sb.sv`, `mac_cov.sv`, `test/mac_rx_basic_test.svh` | Added passive APB RAL prediction, APB functional coverage, APB-driven PAUSE reference expectations, 4-state-safe scoreboard compares, and an independent valid-RX baseline test. | Close the remaining model/checker/coverage gaps and prove normal RX behavior separately from PAUSE traffic. | Generic UVM predictor/subscriber/analysis-port patterns; protocol behavior is MAC-specific. | APB monitor is now the shared passive source for RAL mirror, reference-model configuration state, and coverage; PAUSE expectations are queued at soft-request time. | Medium: configuration/CDC timing and new checker connectivity. | Compile PASS; RX basic PASS (`rx_matches=1`); PAUSE TX PASS (`tx_matches=1`); final WSL regression PASS 11/11. | DONE |
| 6 | `src/hvl_top/tb/mac_cov.sv`, `sim/Questasim/Makefile` | Validated APB functional coverage collection and HTML reporting. | Make configuration-space coverage observable alongside existing TX/RX covergroups. | Generic coverage subscriber/UCDB/report flow. | `make run_cov TEST=mac_register_access_test_c` generated a UCDB and `make cover_report` generated HTML. | Low: coverage instrumentation only. | PASS; `TOTAL COVERGROUP COVERAGE: 19.70%` for the APB-focused baseline run. | DONE |
| 6 | `sim/Questasim/Makefile` | Added `coverage_merge` to combine per-test MAC UCDBs and generate a consolidated HTML report. | Support regression-level coverage reporting without manual simulator commands. | Generic coverage merge/report workflow. | MAC-local `COV_INPUTS`, `COV_MERGED_DB`, and `COV_MERGED_HTML` variables. | Low: automation only. | PASS: merged the APB coverage UCDB to `coverage/handover_merged.ucdb` and generated `coverage/handover_html/index.html`. | DONE |

## Pending implementation entries

Phase 2 onward entries will record exact source/script changes, rationale, risk, and compile/smoke/regression/coverage evidence immediately after each validated modification.
